--find-links https://download.pytorch.org/whl/torch_stable.html

# Core deep-learning stack -- paper Sec. 4.4: PyTorch 2.0.1 + CUDA 11.8 +
# PyG 2.5.x + DGL.
torch==2.3.1+cu118
torch_geometric==2.5.3
dgl==2.2.1

# Numerics / IO
numpy==1.26.4
pandas==2.0.3
scikit-learn==1.3.2
matplotlib==3.7.5
seaborn==0.13.2
tqdm==4.66.4

# Network feature extraction (paper Sec. 3.1: NFStream-based extractor).
nfstream==6.5.3

# Optional: explainability stack (Integrated Gradients via Captum is
# available, our IG_Explainer.py also has a self-contained implementation).
captum==0.7.0

# Misc
typing_extensions==4.12.2
sympy==1.12.1
regex==2024.5.15
